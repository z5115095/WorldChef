<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="460dp"
        android:layout_height="150dp"
        android:background="@drawable/ticketheader">

        <ImageView
            android:id="@+id/learn_quiz_image"
            android:layout_width="246dp"
            android:layout_height="43dp"
            android:layout_marginStart="30dp"
            android:layout_marginTop="90dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/yellowroundbutton" />

        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Play Quiz"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/learn_quiz_image"
            app:layout_constraintEnd_toEndOf="@id/learn_quiz_image"
            app:layout_constraintStart_toStartOf="@id/learn_quiz_image"
            app:layout_constraintTop_toTopOf="@id/learn_quiz_image" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <SearchView
        android:id="@+id/search_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorDEFAULTScheme"
        android:iconifiedByDefault="false">

    </SearchView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/learn_rv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorBACKGROUND">

    </androidx.recyclerview.widget.RecyclerView>

</LinearLayout>